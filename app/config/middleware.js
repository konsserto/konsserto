/**
 *
 * pattern falcultatif, si non présent, il vaut *. Les middlewares sont executé dans l'ordre ci-dessous
 */

middlewares = [
   // {name: "MyMiddleware", middleware: "MFTestBundle:Hello:hello"}
];

module.exports = middlewares;