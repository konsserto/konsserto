/**
 * Created by Marvin on 08/01/2015.
 */
var assert = require('assert');

describe("Array contains", function () {
    it('should return-1 when the value is not present', function () {
        assert.equal(false, false);
    });
});

describe("Array contains", function () {
    it('should return-1 when the value is not present', function () {
        assert.equal(false, false);
    });
});

describe("Array contains", function () {
    it('should return-1 when the value is not present', function () {
        assert.equal(false, false);
    });
});

describe("Array contains", function () {
    it('should return-1 when the value is not present', function () {
        assert.equal(false, false);
    });
});