var Waterline, WaterlineConfig, w;

Waterline = require('waterline');

WaterlineConfig = require('used')('/app/config/waterlineConfig');

w = new Waterline();

w.initialize(config, function(err, models) {
  if (err) {
    throw err;
  }
});
